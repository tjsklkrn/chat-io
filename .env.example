PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/chat-io
JWT_SECRET=change_this_to_a_random_secret
NODE_ENV=development

# Set to true for local dev without installing MongoDB.
# Set to false when using a real MongoDB instance (Homebrew, Atlas, etc.).
USE_MEMORY_DB=true
